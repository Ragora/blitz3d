CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
PROJECT (Compiler)

SET (COMPILER_BUILDLICATION "../compiler")
file(GLOB_RECURSE COMPILER_SOURCES "*.cpp")
file(GLOB_RECURSE COMPILER_INCLUDES "*.h")

ADD_EXECUTABLE (${COMPILER_BUILDLICATION} ${COMPILER_SOURCES} ${COMPILER_INCLUDES})
