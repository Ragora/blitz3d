CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
PROJECT (Linker)

SET (LINKER_BUILDLICATION "../linker")
file(GLOB_RECURSE LINKER_SOURCES "*.cpp")
file(GLOB_RECURSE LINKER_INCLUDES "*.h")

ADD_EXECUTABLE (${LINKER_BUILDLICATION} ${LINKER_SOURCES} ${LINKER_INCLUDES})
