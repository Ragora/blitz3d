CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
PROJECT (Config)

SET (CONFIG_BUILDLICATION "../config")
file(GLOB_RECURSE CONFIG_SOURCES "*.cpp")
file(GLOB_RECURSE CONFIG_INCLUDES "*.h")

ADD_EXECUTABLE (${CONFIG_BUILDLICATION} ${CONFIG_SOURCES} ${CONFIG_INCLUDES})
