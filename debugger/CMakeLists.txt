CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
PROJECT (Debugger)

SET (DEBUGGER_BUILDLICATION "../linker")
file(GLOB_RECURSE DEBUGGER_SOURCES "*.cpp")
file(GLOB_RECURSE DEBUGGER_INCLUDES "*.h")

ADD_EXECUTABLE (${DEBUGGER_BUILDLICATION} ${DEBUGGER_SOURCES} ${DEBUGGER_INCLUDES})
